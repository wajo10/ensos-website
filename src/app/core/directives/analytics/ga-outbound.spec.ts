import { GaOutbound } from './ga-outbound';

describe('GaOutbound', () => {
  it('should create an instance', () => {
    const directive = new GaOutbound();
    expect(directive).toBeTruthy();
  });
});
